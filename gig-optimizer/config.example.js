// Peakr front-end config (optional). Copy to config.js and include it before
// backend-client.js in index.html to turn on the live backend. Without it, the
// app runs standalone on seeded data + localStorage.
window.PEAKR_CONFIG = {
  // Your deployed API base, e.g. the Supabase Edge Functions "api" route:
  apiBase: "https://<PROJECT_REF>.functions.supabase.co/api",
};
